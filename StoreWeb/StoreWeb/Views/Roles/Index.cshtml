@model IEnumerable<ProductStore.Model.UsersRoleViewModel>

@{
    ViewBag.Title = "Roles list";
    Layout = "~/Views/Shared/_ManageLayout.cshtml";
    @Styles.Render("~/Content/css");
}
<div class="panel panel-default">
    <div class="panel-heading">
        <h3>Roles list</h3>
    </div>
    <div id="EditForm">
        @Html.ActionLink("Add new role", "Edit", null,
            new { @class = "btn btn-success" })
    </div>
    <table class="table table-striped table-condensed table-bordered">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserRoleId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserRoleName)
            </th>
           
            <th class="text-center">Action</th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.UserRoleId)
                </td>
                <td>
                    @Html.ActionLink(item.UserRoleName, "Edit", new { item.UserRoleId })
                </td>
                 <td class="text-center">
                    @using (Html.BeginForm("Delete", "Roles"))
                    {
                        @Html.Hidden("Id", item.UserRoleId)
                        <input type="submit"
                               class="btn btn-danger"
                               value="Delete" />
                    }
                </td>
            </tr>
        }
    </table>
</div>