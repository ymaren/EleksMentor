@model IEnumerable<ProductStore.Model.UserViewModel>
 @foreach (var item in Model)
 {
    <tr>
        <td> @Html.DisplayFor(modelItem => item.UserId, new { @class = "text-right" }) </td>
        <td> @Html.ActionLink(item.UserLogin, "CreateChange", new { item.UserId, @class = "text-right" }) </td>
        <td> @Html.DisplayFor(modelItem => item.UserName, new { @class = "text-right" }) </td>
        <td> @Html.DisplayFor(modelItem => item.UserRole.UserRoleName, new { @class = "text-right" }) </td>

        <td class="text-center">
            @using (Html.BeginForm("Delete", "User"))
            {
                @Html.Hidden("UserId", item.UserId)
                <input type="submit"
                       class="btn btn-danger"
                       value="Удалить" />
            }
        </td>
    </tr>
 }